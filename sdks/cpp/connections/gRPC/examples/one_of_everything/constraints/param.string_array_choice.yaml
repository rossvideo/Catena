# STRING_CHOICE constraint example with an array
type: STRING_ARRAY
value: { string_array_values: { strings: [a, b, a] } }
constraint:
  type: STRING_CHOICE
  string_choice:
    choices:
      - a
      - b
    strict: true