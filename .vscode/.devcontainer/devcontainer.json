{
	"name": "Catena-develop",
	"build": {
        "dockerfile": "../../sdks/cpp/docker/dockerfiles/Dockerfile.develop",
        "args": {
            "USER_UID": "${localEnv:UID:1000}",
            // "USER_GID": "${localEnv:DOCKER_GID:1000}",
			// "GROUPNAME": "docker",
            "USERNAME": "${localEnv:USER:developer}"
        }
    }
}
